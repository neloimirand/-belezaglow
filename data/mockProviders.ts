
import { ProviderProfile } from '../types';

/**
 * MOCK_PROVIDERS agora inicia vazio para produção.
 * Os dados reais serão alimentados via Supabase à medida que 
 * profissionais e salões se cadastrarem no ecossistema.
 */
export const MOCK_PROVIDERS: ProviderProfile[] = [];
